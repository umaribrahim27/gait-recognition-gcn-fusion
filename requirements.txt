torch>=2.0
numpy>=1.24
pyyaml>=6.0
pytest>=7.0

# Optional: only needed for live HRNet pose extraction (data/hrnet_extract.py).
# Not required to run the models, tests, or training loop.
# openmim
# mmengine
# mmcv
# mmpose
